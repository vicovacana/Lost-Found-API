services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lostfound
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - lostfound_postgres_data:/var/lib/postgresql/data

volumes:
  lostfound_postgres_data:
